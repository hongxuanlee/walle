var fs = require("fs");
var path = require("path");

var doubi = require("../lib/doubi.js");
describe("doubi: entance", function() {
	it("should run ok.", function() {
		var doubiEngine = doubi.create();
		var tmplPath = path.join(__dirname, "./doubiEntanceTemplate/test1.tmpl");
		var source = fs.readFileSync(tmplPath, "utf-8");
		var target = doubiEngine.compile(source);
	});
});